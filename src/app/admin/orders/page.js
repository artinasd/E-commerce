import { listAdminOrders } from '../../../server/admin/orders.js';
import Link from 'next/link';
const labels={PENDING:'در انتظار',CONFIRMED:'تایید شده',PROCESSING:'در حال پردازش',SHIPPED:'ارسال شده',DELIVERED:'تحویل شده',CANCELLED:'لغو شده',REFUNDED:'مرجوع شده'};
export default async function AdminOrdersPage({searchParams}){const p=await searchParams;const orders=await listAdminOrders({search:p?.search||null,status:p?.status||null,limit:100});return <section dir="rtl"><div className="mb-6"><p className="text-sm font-semibold text-slate-500">فروش</p><h1 className="mt-1 text-3xl font-black">سفارش‌ها</h1></div><form className="mb-5 flex gap-2" method="get"><input name="search" defaultValue={p?.search||''} placeholder="شماره سفارش، ایمیل یا تلفن" className="min-w-0 flex-1 rounded-xl border bg-white px-4 py-3"/><select name="status" defaultValue={p?.status||''} className="rounded-xl border bg-white px-4 py-3"><option value="">همه وضعیت‌ها</option>{Object.entries(labels).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select><button className="rounded-xl bg-slate-950 px-5 py-3 font-bold text-white">فیلتر</button></form><div className="overflow-x-auto rounded-2xl border bg-white shadow-sm"><table className="w-full min-w-[900px] text-right text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-4">سفارش</th><th className="p-4">مشتری</th><th className="p-4">وضعیت</th><th className="p-4">پرداخت</th><th className="p-4">مبلغ</th><th className="p-4">عملیات</th></tr></thead><tbody className="divide-y">{orders.map(o=><tr key={o.id}><td className="p-4 font-mono text-xs">{o.order_number}</td><td className="p-4"><div>{o.email||'—'}</div><div className="text-xs text-slate-400">{o.phone||''}</div></td><td className="p-4 font-bold">{labels[o.status]||o.status}</td><td className="p-4">{o.payment_status}</td><td className="p-4 font-bold">{Number(o.total_amount).toLocaleString('fa-IR')} تومان</td><td className="p-4"><Link href={`/admin/orders/${o.id}`} className="font-bold hover:underline">جزئیات</Link></td></tr>)}</tbody></table>{orders.length===0&&<div className="p-10 text-center text-slate-500">سفارشی پیدا نشد.</div>}</div></section>}
