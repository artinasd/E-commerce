import Link from 'next/link';
import { getAdminCustomer } from '../../../../server/admin/customers.js';
const labels={PENDING:'در انتظار',CONFIRMED:'تایید شده',PROCESSING:'در حال پردازش',SHIPPED:'ارسال شده',DELIVERED:'تحویل شده',CANCELLED:'لغو شده',REFUNDED:'مرجوع شده'};
export default async function AdminCustomerPage({params}){const {id}=await params;const data=await getAdminCustomer(Number(id));if(!data)return <div dir="rtl">مشتری پیدا نشد.</div>;const c=data.customer;return <section dir="rtl" className="space-y-6"><div className="flex items-start justify-between"><div><p className="text-sm text-slate-500">پروفایل مشتری</p><h1 className="mt-1 text-3xl font-black">{[c.first_name,c.last_name].filter(Boolean).join(' ')||'بدون نام'}</h1></div><Link href="/admin/customers" className="rounded-xl border bg-white px-4 py-2 font-bold">بازگشت</Link></div><div className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl border bg-white p-5"><p className="text-xs text-slate-500">ایمیل</p><p className="mt-2 font-bold">{c.email||'—'}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-xs text-slate-500">تلفن</p><p className="mt-2 font-bold">{c.phone||'—'}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-xs text-slate-500">وضعیت حساب</p><p className="mt-2 font-bold">{c.is_active?'فعال':'غیرفعال'}</p></div></div><div className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-black">آدرس‌ها</h2><div className="mt-4 grid gap-3 md:grid-cols-2">{data.addresses.map(a=><div key={a.id} className="rounded-xl border p-4 text-sm"><p className="font-bold">{a.recipient_name} {a.is_default?'— پیش‌فرض':''}</p><p className="mt-1">{a.province}، {a.city}</p><p className="mt-1 text-slate-600">{a.address_line}</p><p className="mt-1 text-xs text-slate-400">{a.postal_code||''}</p></div>)}</div>{!data.addresses.length&&<p className="mt-4 text-sm text-slate-500">آدرسی ثبت نشده است.</p>}</div><div className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-black">تاریخچه سفارش‌ها</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[700px] text-right text-sm"><thead className="border-b bg-slate-50"><tr><th className="p-3">سفارش</th><th className="p-3">وضعیت</th><th className="p-3">پرداخت</th><th className="p-3">مبلغ</th><th className="p-3">عملیات</th></tr></thead><tbody className="divide-y">{data.orders.map(o=><tr key={o.id}><td className="p-3 font-mono text-xs">{o.order_number}</td><td className="p-3">{labels[o.status]||o.status}</td><td className="p-3">{o.payment_status}</td><td className="p-3 font-bold">{Number(o.total_amount).toLocaleString('fa-IR')} تومان</td><td className="p-3"><Link href={`/admin/orders/${o.id}`} className="font-bold hover:underline">مشاهده</Link></td></tr>)}</tbody></table></div>{!data.orders.length&&<p className="mt-4 text-sm text-slate-500">هنوز سفارشی ثبت نشده است.</p>}</div></section>}
