import Link from 'next/link';
import { requireUser } from '../../lib/auth/session.js';
import { getProfile, listAddresses } from '../../server/account/service.js';
import { query } from '../../server/db/connection.js';

export default async function AccountPage(){const user=await requireUser();const [profile,addresses,orders,favorites]=await Promise.all([getProfile(user.id),listAddresses(user.id),query(`SELECT id,order_number,status,payment_status,total_amount,created_at FROM orders WHERE user_id=? ORDER BY created_at DESC,id DESC LIMIT 8`,[user.id]),query(`SELECT f.product_id,p.name,p.slug,p.thumbnail_url FROM favorites f INNER JOIN products p ON p.id=f.product_id AND p.deleted_at IS NULL WHERE f.user_id=? ORDER BY f.created_at DESC LIMIT 8`,[user.id])]);return <main dir="rtl" className="mx-auto max-w-6xl px-4 py-8"><div className="mb-8"><p className="text-sm font-semibold text-slate-500">حساب کاربری</p><h1 className="mt-2 text-3xl font-black">سلام {profile?.firstName||'دوست عزیز'} 👋</h1><p className="mt-2 text-slate-500">مدیریت اطلاعات، سفارش‌ها و آدرس‌های شما</p></div><div className="grid gap-5 lg:grid-cols-3"><section className="rounded-2xl border bg-white p-5 shadow-sm lg:col-span-2"><div className="flex items-center justify-between"><h2 className="text-xl font-black">اطلاعات شخصی</h2><Link href="/account/profile" className="text-sm font-bold">ویرایش</Link></div><div className="mt-5 grid gap-4 sm:grid-cols-2 text-sm"><p><span className="text-slate-400">نام</span><br/><b>{[profile?.firstName,profile?.lastName].filter(Boolean).join(' ')||'ثبت نشده'}</b></p><p><span className="text-slate-400">ایمیل</span><br/><b>{profile?.email||'—'}</b></p><p><span className="text-slate-400">تلفن</span><br/><b>{profile?.phone||'—'}</b></p></div></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="text-xl font-black">آمار حساب</h2><div className="mt-5 grid gap-3 text-sm"><div className="rounded-xl bg-slate-50 p-4"><b>{orders.length}</b><span className="mr-2 text-slate-500">سفارش اخیر</span></div><div className="rounded-xl bg-slate-50 p-4"><b>{addresses.length}</b><span className="mr-2 text-slate-500">آدرس</span></div><div className="rounded-xl bg-slate-50 p-4"><b>{favorites.length}</b><span className="mr-2 text-slate-500">علاقه‌مندی</span></div></div></section></div><section className="mt-5 rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-xl font-black">سفارش‌های اخیر</h2><Link href="/orders" className="text-sm font-bold">مشاهده همه</Link></div><div className="mt-4 divide-y">{orders.map(o=><Link href={`/orders/${o.id}`} key={o.id} className="flex items-center justify-between py-4 hover:bg-slate-50"><span className="font-mono text-xs">{o.order_number}</span><span className="text-sm">{o.payment_status}</span><b>{Number(o.total_amount).toLocaleString('fa-IR')} تومان</b></Link>)}{!orders.length&&<p className="py-8 text-center text-sm text-slate-500">هنوز سفارشی ندارید.</p>}</div></section><section className="mt-5 rounded-2xl border bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-xl font-black">آدرس‌های من</h2><Link href="/account/addresses" className="text-sm font-bold">مدیریت آدرس‌ها</Link></div><div className="mt-4 grid gap-3 md:grid-cols-2">{addresses.slice(0,4).map(a=><div key={a.id} className="rounded-xl border p-4 text-sm"><b>{a.recipient_name}{a.is_default?' · پیش‌فرض':''}</b><p className="mt-1 text-slate-500">{a.province}، {a.city}</p><p className="mt-1 text-slate-600">{a.address_line}</p></div>)}</div>{!addresses.length&&<p className="mt-4 text-sm text-slate-500">هنوز آدرسی ثبت نکرده‌اید.</p>}</section></main>}
