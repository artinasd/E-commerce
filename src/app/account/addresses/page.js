import Link from 'next/link';
import { requireUser } from '../../../lib/auth/session.js';
import { listAddresses } from '../../../server/account/service.js';
export default async function AddressesPage(){const u=await requireUser();const addresses=await listAddresses(u.id);return <section dir="rtl"><div className="mb-6 flex items-center justify-between"><div><Link href="/account" className="text-sm font-bold">← حساب کاربری</Link><h1 className="mt-3 text-3xl font-black">آدرس‌های من</h1></div></div><div className="grid gap-4 md:grid-cols-2">{addresses.map(a=><article key={a.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between gap-4"><h2 className="font-black">{a.recipient_name}</h2>{a.is_default&&<span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">پیش‌فرض</span>}</div><p className="mt-3 text-sm text-slate-600">{a.province}، {a.city}</p><p className="mt-2 text-sm leading-7">{a.address_line}</p><p className="mt-2 text-xs text-slate-400">کد پستی: {a.postal_code||'—'} · {a.recipient_phone}</p></article>)}{!addresses.length&&<div className="rounded-2xl border bg-white p-10 text-center text-slate-500 md:col-span-2">هنوز آدرسی ثبت نکرده‌اید.</div>}</div></section>}
